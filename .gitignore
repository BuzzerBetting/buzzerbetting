node_modules/
package-lock.json
ledger.db
ledger.db-shm
ledger.db-wal

# oc-scraper — deployed directly to the DO server over SSH, deliberately NOT tracked in this
# shared repo (it does Cloudflare-bypass/cookie-harvesting to reach Oddschecker's real API,
# and this repo is shared with a second, independent operator's instance — see
# PROJECT_CONTEXT.md). It still reads/writes against the app via /api/fixtures and
# /api/oc-cache (server.js), those integration points are tracked normally.
oc-scraper/
